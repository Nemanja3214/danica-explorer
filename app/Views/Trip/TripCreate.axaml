<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="app.Views.TripCreate"
    x:DataType="viewModels:TripCreateViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:app.ViewModels;assembly=app"
    xmlns:views="clr-namespace:app.Views;assembly=app"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <Grid ColumnDefinitions="*" RowDefinitions="*, *, *, *, *">
            <views:MapComponent
                DataContext="{Binding MapVm}"
                Grid.Row="0"
                Height="400" />
            <views:DragNDrop DataContext="{Binding AttractionVm}" Grid.Row="1" />
            <views:DragNDrop DataContext="{Binding HotelVm}" Grid.Row="2" />
            <views:DragNDrop DataContext="{Binding RestaurantVm}" Grid.Row="3" />
            <Button
                Grid.Row="4"
                HorizontalAlignment="Right"
                Margin="30">
                <Border Padding="10">
                    <TextBlock>
                        Save
                    </TextBlock>
                </Border>
            </Button>
        </Grid>
    </ScrollViewer>
    <UserControl.KeyBindings>
        <KeyBinding Command="{Binding SaveCommand}" Gesture="{Binding SaveGesture}" />
        <KeyBinding Command="{Binding UndoCommand}" Gesture="{Binding UndoGesture}" />
    </UserControl.KeyBindings>
</UserControl>
